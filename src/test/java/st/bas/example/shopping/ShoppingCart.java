package st.bas.example.shopping;

/**
 * Example for a ShoppingCart schema
 */
public final class ShoppingCart {
    /*
     * Enumeration of all the supported columns when searching for records in the database schema
     * of the example domain of shopping carts in the database of a web shop.
     * <p>
     * This class encompasses the Type-safe projection of the intermediary resultset of the queries
     * <p>
     * Note: We use the the typesafe-enum-pattern and not the Java enum declaration since
     * we want to use Generic Type Parameters.
     * <p>
     */

//    public static final ConditionColumn<Long> CART_ID = new ConditionColumn(CART_LEVEL, CART, "cart_id");
//    public static final ConditionColumn<UUID> CART_LAST_SESSION_ID = new ConditionColumn<>(CART_LEVEL, CART, "last_session_id");
//    public static final ConditionColumn<String> CUSTOMER_LAST_NAME = new ConditionColumn<>(CUSTOMER_LEVEL, CUSTOMER, "last_name");
}
